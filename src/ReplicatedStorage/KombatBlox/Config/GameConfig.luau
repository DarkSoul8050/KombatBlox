--!strict
-- GameConfig v1.0a: constantes globales (cliente + servidor). Solo datos.
-- Los valores de partida viven en Modules/MatchConfig (unica fuente).
return table.freeze({
	Version = "1.0.0",
	Debug = true,
})
